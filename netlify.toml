[build]
  functions = "functions-build"
  # This will be run the site build
  command = "npm run build"
  # This is the directory is publishing to netlify's CDN
  publish = "build"

[template.envionment]
  FAUNADB_SECRET = "fnADI41vYBACBb_f7bTwhvQkyClGNcyzl8WHHfhN"
